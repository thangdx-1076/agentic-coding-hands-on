# Baseline trước khi sửa — 260910-1951

Branch `fix/screen-audit-spec-test-gaps` ← `origin/main` (aeb207c). Tree sạch.

## Gate (đúng lệnh CI dùng)

| Gate | Lệnh | Kết quả |
|---|---|---|
| Typecheck | `pnpm typecheck` | PASS (0 lỗi) |
| Lint strict | `pnpm lint --max-warnings 0` | PASS (0 warning) |
| Format | `pnpm format:check` | PASS |
| Unit | `pnpm test:unit` | **808 passed / 83 files**, 9.88s |
| E2E | `pnpm test:e2e` (full, có `@auth`+`@local-db`) | **217 passed / 5 skipped / 0 failed**, 1.4m |

Supabase local `saa-app` đang chạy (`http://127.0.0.1:55321`) nên lượt e2e này rộng hơn CI —
CI chạy `--grep-invert "@auth|@local-db"`.

## 5 test bị bỏ qua — phân loại

| Test | Lý do | Đánh giá |
|---|---|---|
| `login.spec.ts:592` outage `/login` | skip khi Supabase up; chỉ chạy ở CI | hợp lệ, có chủ ý |
| `login.spec.ts:620` outage `/todo` | như trên | hợp lệ |
| `awards.spec.ts:120` outage `/awards` | như trên | hợp lệ |
| `notifications.spec.ts:833` TC-014 admin moderation | out-of-scope, giữ chỗ | hợp lệ |
| `kudos.spec.ts:739` `[C26]` Own kudo: heart disabled | `fixme` — *"Compose Kudo dialog not implemented"* | **LỖI THỜI**: dialog đã có (F009), test này giờ chạy được |

## Kết luận baseline

Code hiện tại xanh toàn bộ. Mọi thứ audit tìm ra là **thiếu**, không phải **vỡ**.
Regression là rào cứng: 808 unit + 217 e2e phải giữ xanh sau khi sửa.
