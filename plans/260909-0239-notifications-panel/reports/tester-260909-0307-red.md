# Phase 1 RED — Notifications Panel E2E Test

**Status:** NEEDS_CONTEXT  
**Date:** 2026-09-09 03:07  
**Evidence:** pending (test execution required)

## Summary

Completed test authoring for 20 test cases (21 TC − TC-014 out-of-scope). Tests written to RED specification per phase-01-e2e-red-notifications.md. Ready to run but shell execution unavailable in this context.

## Deliverables

**Files Created:**
- `tests/e2e/notifications.spec.ts` (423 lines, 20 test cases)
- `tests/e2e/helpers/seed-notifications.ts` (helper functions for seed/cleanup)

**Test Structure:**
- **No-seed group (TC-001, 006, 008)**: 3 tests
  - TC-001: no-session → panel closed
  - TC-006: panel title + mark-all-read button visible ← **PRIMARY RED ASSERTION**
  - TC-008: empty state (0 notifications)
- **Seed group (TC-002, 003-005, 007, 009, 015, 016-017, 018, 019, 020)**: 12 tests
  - Badge behavior (3 tests)
  - Type rendering + RLS + language + link (5 tests)
  - Mark-read persistence + pagination + realtime + authorization (4 tests)
- **Emitter group (TC-010-013, 021)**: 5 tests
  - Trigger behavior: kudos emit, self-kudo suppression, anonymous payload, dedupe, error handling
- **Skipped**: TC-014 (out-of-scope admin moderation, marked `test.skip` with reason)

## Test Coverage Detail

| TC | Group | Status | Expected RED (Phase 1) |
|---|---|---|---|
| TC-001 | no-seed | pass | N/A (panel closes for no-session) |
| TC-006 | no-seed | **FAIL** | **assertion fail**: panel missing title "Thông báo" + button "Đánh dấu đọc tất cả" |
| TC-008 | no-seed | pass | no "Xem thêm" visible when 0 items |
| TC-002 | seed | fixture error | `relation "notifications" does not exist` |
| TC-003/004/005 | seed | fixture error | badge seed depends on schema |
| TC-007 | seed | fixture error | 4-type seed depends on schema |
| TC-009 | seed | fixture error | i18n test needs seed |
| TC-015 | seed | fixture error | link test needs seed |
| TC-016/017 | seed | fixture error | mark-read seed depends on schema |
| TC-018 | seed | fixture error | pagination seed (15 items) needs schema |
| TC-019 | seed | fixture error | realtime seed needs schema |
| TC-020 | seed | fixture error | auth test seed needs schema |
| TC-010/011/012/013/021 | emitter | fixture error | emitter trigger depends on schema |
| TC-014 | skipped | skipped | admin moderation deferred |

## RED Assertion (TC-006)

File: `tests/e2e/notifications.spec.ts:95–130`

```typescript
// TC-006: Check for title "Thông báo"
const panelTitle = dialog.locator("text=Thông báo");
await expect(panelTitle).toBeVisible();

// TC-006: Check for "Đánh dấu đọc tất cả" button
const markAllReadBtn = dialog.getByRole("button", {
  name: /Đánh dấu đọc tất cả/,
});
await expect(markAllReadBtn).toBeVisible();
```

**Why this is valid RED:**
- Component `notification-bell.tsx:82-89` currently renders only `{emptyStateText}` (empty string)
- No title, no button → assertion fails
- This is a genuine feature gap, not a schema/config issue
- Passes once UI layer implements panel content

## Next Steps Required

1. **Dev server start** (if not running):
   ```bash
   cd /Users/dang.xuan.thang/Desktop/learnings/mock-project/agentic-coding-hands-on
   pnpm dev
   ```

2. **Export service-role key**:
   ```bash
   export SERVICE_ROLE_KEY=$(supabase status -o env | grep SERVICE_ROLE_KEY | cut -d= -f2)
   ```

3. **Run test command**:
   ```bash
   pnpm test:e2e tests/e2e/notifications.spec.ts
   ```

4. **Capture evidence** (automated by runner):
   - Real exit code (non-zero expected)
   - Log file with TC-006 failure verbatim
   - Evidence recorded to `plans/260909-0239-notifications-panel/evidence/red-<ts>.json`

## Known Issues / Deviations

**None.** Phase file honored exactly:
- Header + policy + TC mapping ✓
- Tag `@auth @local-db` on describe block ✓
- 20 tests for 21 TC (−TC-014) ✓
- No-seed group for RED gate ✓
- Seed tests written full (fail fixture, will pass phase 02/03) ✓
- TC-014 skipped with reason ✓
- Service-role key via helper (not hardcoded) ✓
- Zero changes to `src/`, `supabase/`, `messages/` ✓

## Blockers

- **Shell execution** — tester context lacks bash/exec tool
- **User action required** — run script at `/private/tmp/.../run-e2e-red.sh` or commands above

**Status:** NEEDS_CONTEXT
