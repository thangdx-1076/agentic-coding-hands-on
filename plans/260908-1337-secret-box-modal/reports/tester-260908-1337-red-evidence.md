# E2E Red Evidence — Secret Box Modal (MoMorph J3-4YFIpMM)

**Date:** 260908-1337  
**Policy:** e2e-red-first  
**Status:** VALID RED — Feature not yet implemented  

## Red Test Files & Execution

**Test File:**
```
tests/e2e/secret-box.spec.ts
```

**Exact Command:**
```bash
pnpm run test:e2e tests/e2e/secret-box.spec.ts
```

**Real Exit Code:** `1` (non-zero — test suite failed as expected)

## Red Assertion Results

### Primary RED Assertion: FAILED ✗

**Test Name:** `[RED ASSERTION] Secret Box modal [data-testid=secret-box-dialog] must exist`  
**Location:** `tests/e2e/secret-box.spec.ts:107–117`  
**Tag:** `@ci-safe`  

**Error Output:**
```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('[data-testid=secret-box-dialog]')
Expected: 1
Received: 0
Timeout:  5000ms
```

**Cause:** The Secret Box modal component (`[data-testid=secret-box-dialog]`) does not exist in the DOM on `/kudos`. Feature has not been implemented yet.

**Why This is Valid RED:**
- ✓ Assertion failure is due to MISSING FEATURE, not config/dependency issues
- ✓ Element locator is correct and follows repo conventions
- ✓ Test runs cleanly without errors (only assertion failure)
- ✓ Dev server, Supabase, and browser stack all operational

---

## Supporting Tests

### Passed Tests (Design Checks): 2 PASSED ✓

**[S01] Modal does NOT exist when unopened=0**  
- Confirms feature absence when count is hardcoded to 0
- PASSED — correct behavior (no modal when unopened=0)

**[S15] Anonymous viewer: no kudos-open-gift button at all (regression guard, C09)**  
- Regression guard — anonymous users must never see the button
- PASSED — KudosStatList correctly returns null for anonymous viewers
- Prevents regression on `kudos.spec.ts:43 [C09]`

### Skipped Tests: 11 SKIPPED ⊘

**Tests S02–S12** are marked with `test.skip()` because they require:
1. Migration 0011 (secret_box_openings table + open_secret_box RPC)
2. KudosStats to return real unopened box counts (not hardcoded 0)

Each is structured as a durable assertion that will run GREEN once the feature lands.

---

## Test Summary

```
Total tests run: 14
  Passed:  2
  Failed:  1 (RED assertion)
  Skipped: 11 (blocked by migration 0011)
```

**Duration:** ~7.1s  
**Test Class:** Playwright (chromium)

---

## MoMorph Test-Case Mapping

**Primary RED corresponds to:** `84a5ba82` (access control: modal opens for entitled users)

**Red assertion checks:** Modal component must render when feature is implemented

**Secondary assertions (skipped, will activate post-migration 0011):**
| TC ID | Description |
|-------|---|
| a0cd2f27 | Title exact static text |
| a891383a / d9d6e01a | Instruction line display + hidden when count=0 |
| 4bbf0b67 / 56da7ec8 | Badge image displayed, no detail loss |
| 3a8ac6b5 / ce44f5ed | Counter label + backend value |
| 632c600b / 982ae7f9 | Close button position + closes modal |
| 7c3c912f | Click box → badge reveal, count decrements |
| 2a8a63de | Click disabled when count=0 |
| 5cc072ad / 2e7bec78 | Client-side tampering ignored |

---

## Implementation Blockers

Feature cannot render GREEN until:

1. **Migration 0011 exists** — creates `secret_box_openings` table + RLS + `open_secret_box(user_id) RETURNS badge_key` RPC
2. **KudosStats returns real counts** — replaces hardcoded `secretBoxOpened: 0, secretBoxUnopened: 0` in `kudos/page.tsx:146–147`
3. **Secret Box modal component exists** — renders dialog with title, instruction, box, badge, counter, close button
4. **Server action + hook** — wraps `open_secret_box` RPC, handles optimistic updates, count decrement

---

## Unresolved Questions

None. All acceptance criteria and test-case mappings resolved via `clarifications.md`.

---

## Next Steps

1. ✓ **RED captured** — implementation team now has durable E2E spec
2. → **Track A (UI):** Implement `src/app/(public)/kudos/_components/secret-box-dialog.tsx` + hook
3. → **Track B (backend):** Migration 0011, RPC, server action, real KudosStats counts
4. → **Tester:** Re-run `pnpm run test:e2e tests/e2e/secret-box.spec.ts` → expected result GREEN (1 passed, 11 skipped still active)

**Report generated:** 260908-1350  
**Generated by:** tester agent (e2e-red-first policy)
