{
  "task": "Audit the UI of all 5 designed screens (Login, Home, Awards, Standards, Profile) against their MoMorph design frames and fix the genuine deviations. /todo has no design frame (F001 scaffold) and is out of scope. Three deviations found and fixed: the Standards hero-tier row stacked vertically instead of one row, the /awards caption was lower-cased against its own design node, and the nav label used the design node's itemName instead of its character.",
  "mode": "interactive",
  "acceptanceCriteria": [
    "badge+condition now share one row",
    "next/image width/height still sourced from HERO_TIERS table",
    "description spacing unaffected",
    "'Sun* Annual Awards 2025' now matches design node 313:8454",
    "'Award Information' now applied consistently",
    "Diff scope check"
  ],
  "touchpoints": [
    "src/app/(public)/standards/_components/hero-badge-tier-row.tsx",
    "src/app/(public)/awards/_shared/awards-copy.ts",
    "src/app/(public)/(home)/_shared/home-copy.ts",
    "src/app/(public)/(home)/_components/widget-button.tsx",
    "src/app/_shared/site-chrome.ts",
    "src/app/_components/nav-link.stories.tsx",
    "messages/vi.json",
    "messages/en.json",
    "tests/e2e/awards.spec.ts",
    "package.json"
  ],
  "blastRadius": [
    "src/app/_shared/site-chrome.ts feeds SiteHeader and SiteFooter on every page, so the nav-label change is visible on /, /awards, /standards, /login, /todo and /profile",
    "messages/{vi,en}.json are covered by messages-parity.test.ts, which asserts the two bundles stay structurally identical",
    "hero-badge-tier-row.tsx renders 4 times on /standards; its height change affects whether the 6-icon Secret Box grid falls above or below the fold, which contract C9 checks at 1440x2400",
    "the /awards caption is asserted verbatim by tests/e2e/awards.spec.ts TC ID-4",
    "home's caption was deliberately left lower-case, so home.spec.ts must stay green against the unchanged string"
  ],
  "contracts": [
    "MoMorph design frames are authoritative: node `character` is the content, `itemName` is only a designer's layer label",
    "tests/e2e/standards.spec.ts C1-C14, tests/e2e/awards.spec.ts, tests/e2e/home.spec.ts",
    "docs/vi/features/F004_AwardSystemPage and F005_StandardsRulesPage content contracts"
  ]
}
