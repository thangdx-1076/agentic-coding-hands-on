# Doc sync — Homepage SAA (260906-0042-homepage-saa-page)

Source verified in code before writing (not assumed): `app/page.tsx`, `app/home-client.tsx`,
`app/login/{page,login-client}.tsx`, `proxy.ts`, `lib/supabase/next-path.ts`,
`lib/supabase/users-role-client.ts`, `lib/auth/get-user-role.ts`, `lib/auth/sign-in-with-google.ts(+.test)`,
`lib/countdown/countdown.ts`, `hooks/use-countdown.ts`, `hooks/use-select-locale.ts`,
`hooks/use-menu-keyboard-nav.ts`, `mocks/handlers.ts`, `components/home/{home-screen,header}.tsx`,
`components/home/home-copy.ts`, `tests/e2e/{home,login}.spec.ts`, `playwright.config.ts`,
`public/home/*` (19 files), `.env.local` (present, no `.env.example` in this repo).

## Files updated

- `README.md` — intro (public `/`), routes table (`/` public, callback default `/`, guard scope narrowed to `/login`+`/todo`), Setup (`EVENT_START_AT` + `.env.local` sample), CI section (`@auth` admin provisioning via `promote-to-admin.ts`/`supabase db query`/`SAA_APP_DIR`), Assets (`public/home/*`, 19 files), Docs (added F003 link).
- `docs/vi/features/F003_Homepage/README.md` — **created**, matching F001/F002's nav shape; one screen row → `screens/SCR003_Home/spec.md` (verified exists).
- `docs/vi/features/F001_GoogleOAuthLogin/README.md`, `docs/vi/features/F002_LanguageSwitch/README.md` — fixed pre-existing broken link `screens/SCR001_LoginScreen/spec.md` → `screens/SCR001_Login/spec.md` (dir name mismatch flagged in the brief; judged a safe surgical link fix, applied to both).
- `docs/vi/generated/screen-list.md` — SCR001 description `/todo`→`/`; SCR001 Related Screens now points to SCR003; enriched SCR003_HomeScreen with full Components (14, matches index)/Data Displayed/Routes/Related Screens sections mirroring SCR001/SCR002's shape; fixed Cross-Reference Validation (3 screens, no more "FeatureList chưa tồn tại" stale line).
- `docs/vi/generated/screen-flow.md` — Navigation Map + Authentication Flow + Feature Entry Points (added F003 block, fixed F001's stale "Exit screens: SCR002" → SCR003) + Screen Access Paths + Screen Transitions (added SCR003 subsection, rewrote SCR001 exit / SCR002 entry — SCR002 now reachable only by direct URL) + Error Handling Flows (added SCR003 fail-open rows) + Circular Dependencies + Guard Logic (GUARD-001 matrix rewritten, GUARD-002 target fixed, **GUARD-004 marked SUPERSEDED** — `/` no longer redirects) + Deep-Link note (callback default `/todo`→`/`).
- `docs/vi/generated/permissions-matrix.md` — **PERM001_RootRouteGuard marked SUPERSEDED** throughout (index, section, permission rules table, related modules) without deleting the code/mã; PERM002 redirect target `/todo`→`/`; PERM004 fallback target `/todo`→`/`; added "Role-based screen-permission (chưa cấp mã PERM###)" section per brief (no new PERM### invented); fixed Summary/Cross-Reference to include SCR003 and drop a stale "FeatureList chưa tồn tại" line.
- `docs/vi/generated/route-list.md` — `/` now documented as rendering `HomePage`/SCR003_HomeScreen (not `root-redirect`); callback default `next` `/todo`→`/`; proxy redirect matrix table rewritten to match narrowed predicates.
- `docs/vi/generated/entities.md` — MODEL002_SupabaseUser: added `id` and `role` attribute rows (source: `get-user-role.ts:49-68` via the `users-role-client.ts` shim, cited with its TS2589 rationale), added DISC-002 discriminator, updated ERD mermaid + honest-scope note.
- `docs/vi/generated/feature-list.md` — F001 description `/todo`→`/` (kept `/todo` as still-valid protected area); fixed stale Summary counts (Total Screens 2→3, added SCR003/F003 notes) and Cross-Reference Validation lines that hadn't been updated when F003 was promoted (also fixed a stale citation to non-existent `data-model.md`→`entities.md`).
- `docs/vi/generated/traceability-matrix.md` — added F003 row (SCR003; US/BL/ROUTE/PERM marked TBD, no codes invented); marked F001's PERM001 cell "(superseded)"; **fixed a pre-existing drift**: F002's PERM### cell said `PERM001` but `feature-list.md`'s own F002 section (the column's stated source per this file's header) says "Related Permissions: Không có" — corrected to `—`.
- `docs/vi/features/F001_GoogleOAuthLogin/functional-spec.md` — FR-401, US002 AC, § 8 Scenario, § 13 Configuration: landing target `/todo`→`/` (kept as row/one-liner edits, no structural changes).
- `docs/vi/features/F001_GoogleOAuthLogin/technical-spec.md` — targeted fixes bounded to the 2-3 files named in the brief (`app/login/*`, `lib/supabase/next-path.ts`) plus `proxy.ts`/`app/page.tsx` where a citation had gone stale: A1 rule, A2 request/rule, A3 DEC-002 row, A0 cross-cutting rule + Action Index row, § 3.2 edge case, § 5.1 SC-003/US002 Independent Test, mermaid diagram arrow. **A6 (`GET /` fallback) marked SUPERSEDED** end-to-end (Action Index, action body, Components table, § 5.4 Source References) since `app/page.tsx` no longer contains that code at all — this is a citation-pointing-at-moved/deleted-code fix, not new authoring. **SC-007 marked SUPERSEDED** (its test was deleted; real coverage now lives in `home.spec.ts` TC ID-0). § 5.3 item 3 marked moot (root guard's old try/catch question no longer applies — `getViewer()` has try/catch).
- `docs/vi/screens/SCR001_Login/spec.md` — Exit Conditions `/todo`→`/`; Navigation § 8 Exits row destination `SCR002_Todo`→`SCR003_Home` with a real source citation (`app/login/page.tsx:33-36`, replacing "TBD (draft)").
- `docs/vi/system/architecture.md` — reconciled forward-draft → as-built (status was already `implemented` but body still said `(planned)`/`forward-draft` ~15 times): scope line, mermaid diagram (added `HomeClient`/`RoleShim`/renamed nodes), guard paragraph (predicate citations updated to current line numbers), tech-stack Database row, allowlist note (+`users-role-client.ts`), data-flow mermaid + prose (added `getViewer()`/`resolveTargetIso()`/`getInitialNowMs()` citations), test-topology table, MSW paragraph, Storybook paragraph, Playwright section (test counts: 30 login / 27 home / 48 CI-safe of 57 total, verified by grep), CI section (confirmed via `ci.yml` grep that `EVENT_START_AT` is still **not** set for the build step — reworded from "planned" to a real open gap, not fabricated as done).
- `docs/vi/system/permissions.md` — same forward-draft→real reconciliation (frontmatter/scope line, Authorization System Type, Identified Roles, Access Boundaries PERM001 paragraph, Special Conditions ×2); removed the self-referential "this draft can't touch `docs/generated/*`" line since that reconciliation is exactly what this session's `permissions-matrix.md` edit just did.

## Skipped (not edited) + why

- `docs/vi/generated/user-stories.md` — not touched per instruction. **Advisory: run `/tkm:rebuild-spec --features F003`** to allocate real US### codes (F003's functional-spec.md § 7 currently has none).
- `docs/vi/features/F003_Homepage/functional-spec.md`, `technical-spec.md` — already promoted/reconciled per the brief; read for cross-checking only, not edited. Noted (not fixed): `functional-spec.md` frontmatter still says `status: draft` while `technical-spec.md` says `status: implemented` — flagging only, since flipping `status:` is explicitly the orchestrator's Stage 6.b job, not mine.
- `docs/vi/screens/SCR003_Home/spec.md` — out of the file list I was given for this pass; left untouched (still `status: draft`, pre-implementation shape — that's promote-cleanup's job).
- Entities.md's optional "note HomeCopy as a static shape like MODEL003_LoginCopy" — skipped as explicitly optional in the brief, to keep the file focused on the one required change (the `role` field).
- `docs/project-overview-pdr.md`, `codebase-summary.md`, `code-standards.md`, `system-architecture.md`, `project-roadmap.md`, `deployment-guide.md`, `design-guidelines.md` — none exist in this repo (confirmed via `ls`); this project's narrative docs are `README.md` + the `docs/vi/` layered tree only (matches memory `saa2025-docs-layout`).
- `docs/.spec-promote-pending.json`, `docs/vi/.rebuild-state.json`, `docs/vi/_canonical-fcodes.json`, `docs/vi/_source-to-fcode.json` — untouched per instruction (already showed as modified/new before I started; that's the promote pipeline's own output, not mine).

## Advisories (non-blocking)

1. `/tkm:rebuild-spec --features F003` — allocate real US###/BL###/ROUTE###/PERM### codes for F003 (currently TBD in `feature-list.md`, `traceability-matrix.md`, `permissions-matrix.md § Role-based screen-permission`).
2. `/tkm:rebuild-spec --features F001,F003` — the deeper structural cleanup I deliberately did NOT do surgically: formally move `FR-101`/`FR-602`/`PERM001`/A6 out of F001's spec pair entirely now that `app/page.tsx` belongs to F003. I marked all of it SUPERSEDED in place (safer, reversible, no invented codes) rather than deleting/renumbering — a Core pass should do the real reallocation.
3. `docs/vi/features/F003_Homepage/functional-spec.md` frontmatter (`status: draft`) vs. its sibling `technical-spec.md` (`status: implemented`) — inconsistent; not fixed here (Stage 6.b's job per the brief).
4. CI gap, confirmed by reading `.github/workflows/ci.yml`: it still does not set `EVENT_START_AT` for the `quality` job's `build` step (only the two `NEXT_PUBLIC_SUPABASE_*` placeholders are set). Not fatal (countdown just shows `00/00/00`), but worth a follow-up PR since `architecture.md` now flags it as a real, current gap rather than a forward-draft note.

**Status:** DONE
**Summary:** Updated 15 existing docs + created 1 new file (`F003_Homepage/README.md`) across README, generated inventories, F001's two specs, one screen spec, and the two system docs; reconciled two forward-drafts (`architecture.md`, `permissions.md`) to as-built; fixed 3 pre-existing drifts found along the way (broken screen-spec links in 2 READMEs, a stale `feature-list.md`/`traceability-matrix.md` PERM mismatch for F002, a dead `data-model.md` citation). No new spec codes invented; PERM001/A6/SC-007 marked superseded in place rather than deleted or renumbered.
**Concerns/Blockers:** None blocking. See Advisories above for follow-up work outside this agent's edit scope.
