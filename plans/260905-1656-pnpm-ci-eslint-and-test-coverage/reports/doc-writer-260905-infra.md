# Doc review — pnpm/ESLint/CI/coverage infra session

Scope: README.md, AGENTS.md/CLAUDE.md, cross-references, contributor-facing CI/@auth/pnpm gaps,
plus a status check on the flagged feature-spec drift. Read `package.json`, `vitest.config.ts`,
`playwright.config.ts`, `.github/workflows/ci.yml`, `.prettierignore`, `docs/vi/.rebuild-state.json`,
`docs/vi/features/.stale`, `docs/vi/_canonical-fcodes.json`, and the two feature/screen spec files
before writing anything. Did not touch `docs/vi/system/*`, `docs/vi/generated/**`, or the generated
zone of `docs/README.md` (all in bounds — none needed a factual-error report).

## Fixed: README.md (updated, prettier-formatted, 93 lines)

**Scripts table was stale** — verified against `package.json` `scripts`:
- Missing rows for `lint:fix`, `format`, `format:check`, `test:unit:coverage` (all four scripts
  exist and are exercised by `.github/workflows/ci.yml`, e.g. `format:check` is CI's actual check).
- `test:unit` row still named only the two pre-session test files; `lib/i18n/messages-parity.test.ts`
  (new this session, confirmed via `find` — unit count really is 3 files / went 32→34 tests) was
  missing from the description.

Added all four rows plus the missing test file, sourced from `package.json` scripts verbatim —
no invented flags.

**No CI section existed anywhere in README** — grepped for `ci\b|github/workflows|@auth`, zero
hits before this edit. This repo had no CI before this session (per the task brief, confirmed by
`.github/workflows/ci.yml`'s presence and its own "no CI before" framing), so a newcomer had no
way to learn: (a) CI exists, (b) what each of its two jobs gates, (c) that the `e2e` job silently
excludes `@auth`-tagged tests and why. Added one small `## CI` section (6 lines) rather than a new
doc — summarizes the two jobs (`quality`, `e2e`) and points to `ci.yml`'s own file-header comment
for the full coverage-limitation text instead of duplicating it (that comment is detailed and
already correct; restating it in README would just be a second copy to drift).

pnpm-vs-npm prerequisite note (lines 35-39, the nvm/PATH caveat) was already accurate — verified
against the actual `packageManager`/`engines` fields; left as-is.

Ran `pnpm exec prettier --write README.md` after editing (the file is NOT in `.prettierignore`,
unlike the rest of `docs/`) and confirmed `pnpm format:check` now passes clean.

## Checked, no changes needed

**AGENTS.md / CLAUDE.md** — `CLAUDE.md` is a one-line `@AGENTS.md` import. `AGENTS.md`'s content is
the auto-generated Next.js dev-mode banner ("This is NOT the Next.js you know... written and
re-added by `next dev`") — it says nothing about npm, test commands, or lint, and its own text says
edits to it get overwritten by the dev server. Nothing false to fix; nothing here is this session's
concern.

**Cross-references in README's `## Docs` section** — all six paths (`architecture.md`,
`permissions.md`, `feature-list.md`, `screen-list.md`, `F001_GoogleOAuthLogin/`,
`F002_LanguageSwitch/`) verified to exist on disk. No changes needed.

**`docs/README.md`** — fully inside the `<!-- generated by rebuild-spec navigation -->` /
`<!-- end-generated -->` markers, in scope of the hard boundary. It's an index of generated docs
only; nothing about scripts/CI belongs there anyway.

## Flagged, not fixed (per the hard boundary)

`docs/vi/features/{F001,F002}/*` and `docs/vi/screens/{SCR001,SCR002}/spec.md` — confirmed still
`status: draft`, `created: 2026-09-04`. Also confirmed a `docs/vi/features/.stale` marker file
exists reading exactly: `source changed — run /tkm:rebuild-spec --feature-specs to refresh`. So the
remediation path is already self-documented in the repo, not something I'm inferring. One
correction to the brief's framing: the F001/F002 codes themselves are NOT a superseded ID scheme —
`docs/vi/_canonical-fcodes.json` (generated 2026-09-04, same plan run) lists F001/F002 as the
current canonical codes for these two features. The staleness is about spec *content* vs.
source changes, not the code scheme. Remediation is still `/tkm:rebuild-spec --feature-specs`,
unchanged from the brief — hand-editing these files is out of scope for this task regardless.

## Status

**Status:** DONE
**Verdict:** updated 1 file — `README.md` (`/Users/dang.xuan.thang/Desktop/learnings/mock-project/agentic-coding-hands-on/README.md`): added 4 missing script rows + 1 missing test file to the Scripts table, added a new `## CI` section documenting the `quality`/`e2e` jobs and the `@auth` split, re-ran through Prettier so `pnpm format:check` stays green.

No unresolved questions.
