# E2E Test RED Run — Login Screen

**Date:** 2026-09-04  
**Policy:** e2e-red-first  
**Status:** RED (12 genuine assertion failures; 2 skipped fixme tests)

## Deliverables

### testRunner
```
@playwright/test 1.62.1
npx playwright test --reporter=list
```

### redTestFiles
```
- tests/e2e/login.spec.ts (12 unauthenticated tests + 2 authenticated fixme)
- playwright.config.ts (webServer: npm run dev)
```

### redCommand
```bash
npx playwright test tests/e2e/login.spec.ts --reporter=list
```

### redExitCode
```
1 (failure)
```

### redFailure
**All 12 unauthenticated tests FAILED on assertion (element not found):**
1. `[TC b9805e65]` Logo not found: `header img[alt="Sun* Annual Awards 2025"]`
2. `[TC 8415b629]` Language selector: name doesn't contain "VN" (shows "Open Next.js Dev Tools")
3. `[TC 5fbe2a18]` Hero image not found: `img[alt="ROOT FURTHER"]`
4. `[TC 42b82364]` Hero text not found: "Bắt đầu hành trình của bạn cùng SAA 2025."
5. `[TC 6ae76d15]` Google button not found: `button:has-text("LOGIN With Google")`
6. `[TC 33a1dacf]` Footer text not found: "Bản quyền thuộc về Sun* © 2025"
7. `[TC 20d87e28]` Language menu not found (menu role missing)
8. `[TC 45278c06-a]` Error alert not found on `/login?error=auth` (role="alert" missing)
9. `[TC 45278c06-b]` `/todo` redirect fails: 404 (page not yet created)
10. `[TC 45278c06-c]` `/` redirect fails: 404 (page not yet created)
11. `[TC 60bc5bbb]` Google OAuth endpoint never called (button not found)
12. `[TC 37eae882]` Button loading state not observable (button not found)

**Why these fail:** Route `/login` returns 404 (page not yet implemented). Navigation to `/`, `/todo` also fail with 404. These are GENUINE ASSERTION RED, not infrastructure failures.

**2 Authenticated tests marked `test.fixme()`:**
- `[TC f62b0c97]` Authenticated user redirect `/login` → `/todo` (requires auth session injection)
- `[TC e76aa170]` `/todo` protected page with user email + logout (requires auth session)

Reason: `tests/e2e/auth.setup.ts` not yet created. Session injection is deferred; unauthenticated RED is complete and runnable.

### googleProviderCheck
```
Status: 302
Redirect: https://accounts.google.com/o/oauth2/v2/auth?client_id=659560290379-8jedlba3uataajn3fa064b9nqtttk15g.apps.googleusercontent.com&...
Instance: saa-app (local Supabase)
Verified: Google OAuth provider is LIVE and responding correctly
```

### authSetupStatus
- `tests/e2e/auth.setup.ts` not created (deferred)
- `playwright/.auth/user.json` not created (will be generated by setup project post-implementation)
- Unauthenticated tests (chromium-anon project) run independently and produce full RED signal
- Authenticated tests marked fixme; no blocker to implementation

### filesCreated
```
✓ playwright.config.ts
✓ tests/e2e/login.spec.ts (12 + 2 tests)
✓ .env.local (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)
✓ package.json (added "test:e2e": "playwright test")
✓ .gitignore (added /test-results/, /playwright-report/, /playwright/.auth/)
✓ evidence/red-run.log (full Playwright output, exit code 1)
```

### unresolved
- Auth setup: `auth.setup.ts` deferred until app code exists (no blocker to unauthenticated RED)
- Authenticated redirect tests: marked fixme; can be enabled after session injection verification

## Next Steps (for implementer)
1. Create `/login` route (app/login/page.tsx or similar)
2. Render header, hero, footer, form as per MoMorph specs
3. Implement language selector with cookie logic
4. Add `/auth/callback` route handler
5. Implement `/todo` protected placeholder
6. Update middleware/proxy.ts for redirects

Once `/login` renders, run: `npx playwright test tests/e2e/login.spec.ts --reporter=list` → should go GREEN.
