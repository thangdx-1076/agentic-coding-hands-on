# Doc Writer — Delivery Step Review (Polish + Core Docs Pass)

**Date:** 2026-09-05 · **Scope:** post-`/tkm:rebuild-spec` doc-impact review after the U+2028/2029 hardening
(`lib/supabase/next-path.ts`) and ARIA APG keyboard nav (`components/login/language-selector.tsx`) fixes.

## Method

Read every file before describing it: `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/README.md`,
`docs/journals/README.md` + both journal entries, `docs/vi/system/{architecture,permissions}.md`,
`docs/vi/generated/{user-stories,route-list,permissions-matrix,entities,behavior-logic,screen-list,feature-list}.md`,
all 4 feature-spec files (F001/F002 × functional/technical), both screen specs (SCR001/SCR002), the two
changed source files, and `reviewer-260905-polish-defer-inspection.md` for prior-session ground truth.
Cross-checked `package.json` scripts and file existence against README claims.

## 1. Human-owned root docs — verified accurate, no changes made

- `README.md`: routes table, stack table, scripts table, and setup steps all verified against
  `package.json` scripts (`dev`/`build`/`start`/`lint`/`test:unit`/`test:e2e` — exact match) and against
  actual route files (`app/page.tsx`, `app/login/page.tsx`, `app/auth/callback/route.ts`, `app/todo/page.tsx`).
  Its "Docs" section links (`docs/vi/system/architecture.md`, `permissions.md`, `generated/feature-list.md`,
  `generated/screen-list.md`, `features/F001_GoogleOAuthLogin/`, `features/F002_LanguageSwitch/`) all resolve.
  "Known gaps" (`/todo` is a placeholder) is still true. Nothing in this session's two code changes
  (open-redirect hardening, keyboard nav) rises to README's level of detail — no edit warranted.
- `AGENTS.md` / `CLAUDE.md`: framework-generated Next.js banner + `@AGENTS.md` include, unrelated to app
  behavior. No drift.
- `docs/README.md`: generated-navigation stub (inside the `<!-- generated by rebuild-spec navigation -->`
  markers) — hard boundary, not touched.

## 2. Hard-boundary generated layer — confirmed reconciled, no factual errors spotted

`docs/vi/system/architecture.md` and `permissions.md` are now `status: implemented`, dated 2026-09-05,
and correctly code-derived (checked the Mermaid diagrams and the two-layer guard description against
`proxy.ts`, `app/page.tsx`, `app/login/page.tsx`, `app/todo/page.tsx` — accurate). I did **not** find any
other document — human-owned or generated — still quoting or relying on the old "chưa có code" /
forward-draft framing for these two files specifically. The only "chưa có code" phrase left anywhere is
inside `F002_LanguageSwitch/technical-spec.md:122` (see § 3 below — that's the stale-feature-spec drift
itself, not a reference to the old architecture.md framing). Per the hard boundary, no edits made to
`docs/vi/system/**` or `docs/vi/generated/**`.

## 3. Confirmed real drift — feature specs and screen specs still pre-code drafts (report only, not fixed)

All **6 files** below are still stamped `status: draft`, `created: 2026-09-04` (before any code existed),
and are internally consistent with each other but **not** with the new generated layer:

| File | Evidence of staleness |
|---|---|
| `docs/vi/features/F001_GoogleOAuthLogin/functional-spec.md` | Own ID scheme: US001_GoogleLogin…US005_LoginErrorMessage (5 codes, F001-local) |
| `docs/vi/features/F001_GoogleOAuthLogin/technical-spec.md` | Every action marked `(planned)`, every `Source:` is `TBD (draft)`, § 5.4 literally says "No source code written yet" |
| `docs/vi/features/F002_LanguageSwitch/functional-spec.md` | Own scheme: US001_ChuyenDoiNgonNgu, US002_LuuLuaChonNgonNgu |
| `docs/vi/features/F002_LanguageSwitch/technical-spec.md` | `setLocale` marked `(planned)`; § 5.3 unresolved question literally says "chưa xác nhận được vì chưa có code"; § 5.4 "Chưa có source code" |
| `docs/vi/screens/SCR001_Login/spec.md` | Components marked `(planned)`/TBD, e.g. entry E01/E02 "asset ... (TBD draft)" |
| `docs/vi/screens/SCR002_Todo/spec.md` | Same `status: draft`, `created: 2026-09-04` header |

The new canonical generated layer uses a **disjoint, incompatible** numbering scheme built from the
actual 2-screen / 3-interaction-point codebase: `US001_SwitchLanguage`, `US002_LoginWithGoogle`,
`US003_LogOut` (3 global codes, not 5+2 per-feature), `SCR001_LoginScreen`, `SCR002_TodoScreen`,
`ROUTE001` (`/auth/callback`), `MODEL001-003`, `BL001-003`, `PERM001-004`. None of these codes appear
anywhere in the 6 stale files above — e.g. old `US001_GoogleLogin` and new `US002_LoginWithGoogle`
describe the same behavior under different codes.

`docs/vi/features/.stale` already carries the correct instruction: `source changed — run
/tkm:rebuild-spec --feature-specs to refresh`. There is **no equivalent `.stale` marker under
`docs/vi/screens/`**, but both screen specs show the identical pre-code draft symptoms — recommend the
rebuild-spec pass cover screens too, not just features.

Per this session's explicit instruction, I did **not** hand-thread the new codes into these 6 files —
doing so correctly requires real `file:line` citations replacing every `TBD (draft)` `Source:` field,
which is a full authoring pass, not a row-level surgical edit.

**Verdict:** drift confirmed and scoped. Action needed: `/tkm:rebuild-spec --feature-specs` (and ideally
extend to `docs/vi/screens/`) — not a doc-writer hand-edit.

## 4. Flagged, not fixed — stale journal entry (outside doc-writer's write scope)

`docs/journals/260905-1441-login-hero-background-asset-fix.md` § "Còn mở" (Open) lists two items that
**this session's own work already closed**:

1. *"`docs/vi/system/*` vẫn forward-draft (rebuild-spec Core pending, không blocking)"* — resolved: the
   Core pass ran this session (§ 2 above, both files now `status: implemented`).
2. *"Reviewer deferred items không thay đổi"* — resolved: both prior-session deferred items (U+2028/2029
   hardening, ARIA keyboard nav) are implemented and reviewer-approved this session
   (`reviewer-260905-polish-defer-inspection.md`: "Both deferred items are genuinely closed... No critical
   or security-blocking findings").

Journals are dated historical records owned by the `journal-writer` agent, not prose I edit — rewriting a
past entry's "what was open at the time" section would misrepresent the historical record. Recommending a
follow-up journal entry be written (by `journal-writer`) rather than editing the 2026-09-05 14:41 entry in
place.

*(Side note, not a doc problem: the reviewer's inspection also flagged a mouse-reopen stale-`activeIndex`
focus bug as a Warning — the code I read already contains the fix, `handleButtonClick` routes every
pointer-open through `openMenuAt(0)` with a comment explaining exactly why. This is a code-review matter
already closed in code, mentioned here only so the journal follow-up has the full picture.)